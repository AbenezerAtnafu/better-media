# @better-media/adapter-db-memory

In-memory `DatabaseAdapter` for Better Media (development and testing).

```ts
import { memoryDatabase, MemoryDbAdapter } from "@better-media/adapter-db-memory";
```
