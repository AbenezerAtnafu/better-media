# @better-media/adapter-db-kysely

Kysely-backed `DatabaseAdapter` for Better Media (PostgreSQL, MySQL, SQLite).

```ts
import { kyselyAdapter, KyselyDbAdapter } from "@better-media/adapter-db-kysely";
```

Install `kysely` and your driver (`pg`, `mysql2`, or `better-sqlite3`) in your app as needed.
